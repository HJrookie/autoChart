/** default options in LOWESS */
export var DEFAULT_LOWESS_OPTIONS = {
    f: 2 / 3,
    nSteps: 3,
    delta: null,
};
/** default options in LOWESS */
export var DEFAULT_PCORRTEST_OPTIONS = {
    alpha: 0.05,
    alternative: 'two-sided',
    rho: 0,
};
